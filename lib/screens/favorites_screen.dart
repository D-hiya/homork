import 'package:flutter/material.dart';

class FavoritesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('العقارات المفضلة'),
      ),
      body: Center(
        child: Text('ميزة المفضلة قيد التطوير.'),
      ),
    );
  }
}
